'use client';
/**
 * Client-boundary re-export for `CalibrationPage`. Lets the server
 * `app/data/calibration/page.tsx` route render the existing hook-based
 * `CalibrationPage` view without modifying that file.
 */
export { default } from './CalibrationPage';
